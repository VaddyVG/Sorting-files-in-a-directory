[tool.poetry]
name = "sorting files in a directory"
version = "0.1.0"
description = ""
authors = ["VaddyVG <vads1999@inbox.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
